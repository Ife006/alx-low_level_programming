#include "main.h"

/**
 * binary_to_uint - converts a binary number to an unsigned int
 * @b: pointer to binary number to be converted
 * Return: 0 if b is NULL, or even there are chars in the string != 0 or 1.
 * Else, the converted number
 */
unsigned int binary_to_uint(const char *b)
{
	char *b;
	unsigned int i;

	if(*b != 0 & 1 || *b = NULL) 
	{
	return (0);
	}
	else
	binary_to_unit *b;
	{
		return (unsigned int i);

}
