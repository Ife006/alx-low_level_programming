#include "main.h"

/**
 * print_last_digit - prints the last digit of a number
 * @n: the number to be checked
 * Return: m, variable to hold lasy digit of n
 */
int print_last_digit(int n)
{
	int n

	n = m % 10;
	if (i < 0)
	{
		i = i * -1;
	}
	_putchar(i + '0');
	return (m);
}
